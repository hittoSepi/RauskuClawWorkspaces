<UserControl x:Class="RauskuClaw.GUI.Views.Steps.Step4Access"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Workspace Ready"
                   Style="{StaticResource Header}"
                   Padding="0"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1"
                   Text="Save these endpoints for quick access. You can now close the wizard."
                   Style="{StaticResource MutedTextBlock}"
                   Padding="0"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

        <Border Grid.Row="2"
                Background="#101B2D"
                BorderBrush="#1F6FEB"
                BorderThickness="1"
                CornerRadius="4"
                Padding="10"
                Margin="0,0,0,10">
            <StackPanel>
                <DockPanel Margin="0,0,0,4">
                    <TextBlock Text="Access Info"
                               Foreground="#E6EAF2"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"/>
                    <Button
                            Command="{Binding CopyAccessInfoCommand}"
                            DockPanel.Dock="Right"
                            Padding="8,2"
                            Margin="8,0,0,0"
                            Background="#2A3240"
                            Foreground="#E6EAF2"
                            BorderThickness="0"
                            Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <fa:IconBlock Icon="Copy" Style="{StaticResource FaIconInline}"/>
                            <TextBlock Text="Copy" Foreground="#E6EAF2"/>
                        </StackPanel>
                    </Button>
                </DockPanel>
                <TextBlock Text="{Binding AccessInfo}"
                           Style="{StaticResource MutedTextBlock}"
                           Padding="0"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <TextBox Grid.Row="3"
                 Text="{Binding RunLog, Mode=OneWay}"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 TextWrapping="NoWrap"/>
    </Grid>
</UserControl>

